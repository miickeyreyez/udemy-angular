import { PeliculaimagePipe } from './peliculaimage.pipe';

describe('PeliculaimagePipe', () => {
  it('create an instance', () => {
    const pipe = new PeliculaimagePipe();
    expect(pipe).toBeTruthy();
  });
});
