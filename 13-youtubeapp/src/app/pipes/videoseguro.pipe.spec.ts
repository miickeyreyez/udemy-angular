import { VideoseguroPipe } from './videoseguro.pipe';

describe('VideoseguroPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoseguroPipe();
    expect(pipe).toBeTruthy();
  });
});
